# MySQL Gold layer connection (optional — CSV demo works without MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=medallion_dw

# Set to 1 to force CSV fallback even if MySQL is available
USE_CSV=0
